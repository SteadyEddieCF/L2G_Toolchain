# L2G Suite Coordination

This area contains named suite snapshots and optional portable-suite assembly material. A suite snapshot records the exact independently versioned module releases and package contracts tested together.

Draft snapshots must not be represented as validated production combinations.
