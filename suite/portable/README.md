# L2G Portable Suite

Target: a no-install package that opens the Control Center and local HTML modules without administrator rights.

The HTML applications remain the authoritative runtime and fallback. Optional CMD or PowerShell helpers may build, verify, update, back up, and roll back the suite, but the basic toolchain must remain usable without an installer or elevated permissions.
