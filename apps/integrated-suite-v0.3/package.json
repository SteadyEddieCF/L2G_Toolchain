{
  "name": "@l2g/integrated-suite-engagement-spine",
  "version": "0.3.0",
  "private": true,
  "description": "Offline encrypted engagement spine for L2G Integrated Suite.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "python scripts/build.py",
    "validate": "python scripts/validate.py",
    "test:domain": "tsc -p tsconfig.domain-test.json && node scripts/test_domain.mjs",
    "test:determinism": "python scripts/check_determinism.py",
    "test": "npm run typecheck && npm run build && npm run validate && npm run test:domain && npm run test:determinism"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
