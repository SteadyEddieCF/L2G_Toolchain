# L2G Integrated Suite v0.2.0 — Validation Report

## Release identity

- application: L2G Integrated Suite
- version: `0.2.0`
- validated candidate head: `fb431939815eacffda198030c0d63c5b4ccc4f59`
- implementation baseline: `6b35e955e854d3ba5507a7a97f8f9bdaa1cdacec`
- portable artifact: `L2G_Integrated_Suite_Encrypted_Project_v0.2.0.html`
- portable HTML SHA-256: `84526756161fa44bc2dcaebe791a2ea1b73c06341c7563e34693aa6b7231af86`
- portable HTML size: `99505` bytes
- encrypted envelope kind: `l2g_encrypted_project_v1`
- inner project kind: `l2g_project_v1`
- fixed synthetic encrypted vector SHA-256: `e23492e6012f710b2eba94801fa2f863ab20d94f8e65f8e66d2267eeaa7a8d66`

## Candidate workflow evidence

### Integrated Suite Encrypted Project v0.2

Run `30862444620` passed on exact candidate head.

Linux artifact:

- artifact ID: `8874847192`
- Actions artifact digest: `sha256:66d0624a098c44657fab645a9afbfdd6d17d47ed72524586ef67cc42b355c318`

Windows file-origin artifact:

- artifact ID: `8874856252`
- Actions artifact digest: `sha256:cb678436cadef3b5c3a8ffc1104ef00e6146bb8ef20b9ed5524a642e9f51145c`

Direct artifact inspection confirmed:

- the release manifest reports the exact HTML identity above;
- the release package and dist copies are byte-identical;
- 5 of 5 dedicated browser tests passed;
- fixed-vector encryption/decryption passed;
- two runtime encryptions of identical plaintext produced different ciphertext;
- wrong passphrase, purpose replay, and ciphertext tamper were rejected;
- the known synthetic engagement marker was absent from the encrypted outer package;
- Linux and native Windows `file://` builds produced and exercised the portable runtime.

### Existing-suite non-regression

The same candidate head passed:

- Playwright QA run `30862444611` — current six-tool runtime, axe-core, governed routes, visual regression, and native Windows file-origin;
- RG-4 Merged-Main Six-Tool Validation run `30862444632` — static identities, exact governed route matrix, and current Windows routes;
- Validate L2G Toolchain run `30862444608`;
- every triggered SSP v1.7.1 and v1.8.0 through v1.8.5 materializer.

## Cross-platform corrections during candidate development

Two Windows-only build defects were found and corrected before the validated candidate head:

1. the deterministic builder now resolves the locked local `tsc` or `tsc.cmd` compiler rather than assuming a Unix executable name;
2. release source, generated JavaScript, CSS, HTML, manifests, and checksums use explicit UTF-8 reads and writes rather than the Windows locale default.

No acceptance criterion was weakened or skipped.

## Proven behavior

- whole canonical inner project encryption using AES-256-GCM;
- PBKDF2-HMAC-SHA-256 with exactly 600,000 iterations;
- authenticated envelope purpose and metadata;
- encrypted portable save/open round trip;
- encrypted browser recovery with explicit passphrase unlock;
- failed passphrase authentication leaves active governed state unchanged;
- outer package contains only `ciphertext.bin` and `envelope.json`;
- no default client, engagement, or system name in portable filenames;
- restrictive CSP with `connect-src 'none'` and zero runtime network dependencies;
- Advisor, Client, and Reviewer presentation behavior;
- Undo/Redo, checkpoints, history, and current compatibility catalog;
- additive preservation of v0.1.0 and all standalone module releases.

## Final-head gate

The release pointer and this report are metadata-only changes. The portable runtime source and generated bytes are unchanged from the validated candidate. Every required workflow must pass again on the exact final promotion head before PR #127 may leave draft and merge.

## Qualification

This evidence does not authorize production, client, FCI, or CUI data. It does not establish readiness, compliance, certification, scoring, evidence sufficiency, risk, technical accuracy, or Met/Not Met conclusions. Forgotten passphrases cannot be reset or recovered. Encryption does not protect an unlocked project on a compromised endpoint.
