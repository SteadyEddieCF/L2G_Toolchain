# L2G Package Contracts

`registry.json` is the human- and machine-readable package route registry. Stable contract changes require an explicit proposal, producer and consumer examples, compatibility analysis, and cross-tool regression.

Additive optional fields should remain safely ignorable and do not automatically justify a package-version change.
