import { test, expect } from '@playwright/test';
const route='/modules/builder-merger/releases/v3.10.1/L2G-BM_v3.10.1.html';
const frozenChecks=[['WQA-PACKAGE-OPEN','automated','blocking'],['WQA-SOURCE-IDENTITY','automated','blocking'],['WQA-UNRESOLVED-TOKENS','automated','blocking'],['WQA-COMMENTS-REVISIONS','automated','blocking'],['WQA-LAYOUT-HUMAN','human','blocking']];
test('Builder/Merger v3.10.1 preserves SSP Final Word-QA producer',async({page})=>{const errors=[],consoleErrors=[],external=[];page.on('pageerror',e=>errors.push(String(e)));page.on('console',m=>{if(m.type()==='error')consoleErrors.push(m.text());});page.on('request',r=>{if(/^https?:/i.test(r.url())&&!r.url().startsWith('http://127.0.0.1:4173/'))external.push(r.url());});await page.goto(route,{waitUntil:'domcontentloaded'});await page.getByRole('tab',{name:'SSP Final Word-QA'}).click();await expect(page.getByRole('heading',{name:'SSP Final Word-QA Sidecar'})).toBeVisible();expect(await page.evaluate(()=>WQA_PROFILE.checks.map(c=>[c.check_id,c.classification,c.severity]))).toEqual(frozenChecks);expect(await page.locator('meta[http-equiv="Content-Security-Policy"]').getAttribute('content')).toContain("connect-src 'none'");expect(errors).toEqual([]);expect(consoleErrors).toEqual([]);expect(external).toEqual([]);});
