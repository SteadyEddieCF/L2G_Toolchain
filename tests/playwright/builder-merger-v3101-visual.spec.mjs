import { test, expect } from '@playwright/test';
const route='/modules/builder-merger/releases/v3.10.1/L2G-BM_v3.10.1.html';
test('builder-merger-v3.10.1 visual light/dark, constrained viewport, and print surface',async({page},testInfo)=>{const errors=[];page.on('pageerror',e=>errors.push(String(e)));await page.setViewportSize({width:1280,height:900});await page.goto(route,{waitUntil:'domcontentloaded'});await expect(page.locator('h1')).toContainText('v3.10.1');await testInfo.attach('builder-merger-v3.10.1-dark.png',{body:await page.screenshot({fullPage:false}),contentType:'image/png'});await page.getByRole('tab',{name:'SSP Final Word-QA'}).click();await page.getByRole('button',{name:'Light theme'}).click();await expect(page.locator('body')).toHaveClass(/wqa-light-theme/);await testInfo.attach('builder-merger-v3.10.1-light.png',{body:await page.screenshot({fullPage:false}),contentType:'image/png'});await page.setViewportSize({width:375,height:667});expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBe(await page.evaluate(()=>document.documentElement.clientWidth));const pdf=await page.pdf({printBackground:true});expect(pdf.length).toBeGreaterThan(10000);expect(errors).toEqual([]);});
