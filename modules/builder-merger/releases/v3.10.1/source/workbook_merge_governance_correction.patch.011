--- L2G-BM_v3.10.1.before.html
+++ L2G-BM_v3.10.1.after.html
@@ -6375,7 +6375,7 @@
 }
 function v3101ValidateLegacy16(raw){const errors=[],req=(p,v,e)=>{if(v!==e)errors.push(`${p}: expected ${JSON.stringify(e)}, got ${JSON.stringify(v)}`);};req('package_kind',raw.package_kind,'l2g_workbook_handoff_v1');req('package_version',raw.package_version,'1.0');req('handoff_schema_enhancements_version',raw.handoff_schema_enhancements_version,'1.6');req('schema_trusted',raw.schema_trusted,true);req('contract_manifest.contract_name',raw.contract_manifest?.contract_name,'l2g_workbook_handoff_v1');req('contract_manifest.contract_release',raw.contract_manifest?.contract_release,'1.6');req('contract_manifest.required_package_identity.package_kind',raw.contract_manifest?.required_package_identity?.package_kind,'l2g_workbook_handoff_v1');req('contract_manifest.required_package_identity.package_version',raw.contract_manifest?.required_package_identity?.package_version,'1.0');req('package_integrity.contract_release',raw.package_integrity?.contract_release,'1.6');const unknown=Object.keys(raw).filter(k=>!V3101_IDENTITY_ONLY_FIELDS.has(k));if(unknown.length)errors.push(`legacy 1.6 fixture has undeclared fields: ${unknown.join(', ')}`);return {status:errors.length?'rejected':'legacy-1.6-preview',trusted:!errors.length,current:false,governedBuildAllowed:false,identityOnly:true,legacy:true,errors,warnings:['Explicit legacy contract release 1.6 is recognized for non-mutating identity preview only. It is not silently upgraded or used for governed workbook generation.'],practiceCount:0,objectiveCount:0,inertTextCount:v3101InertTextCount(raw),packageFingerprint:raw.package_integrity?.canonical_fingerprint||'',calculatedFingerprint:''};}
 function v3101ValidateHandoff(raw){if(!raw||typeof raw!=='object'||Array.isArray(raw))return {status:'rejected',trusted:false,current:false,governedBuildAllowed:false,errors:['Workbook Handoff must be a JSON object.'],warnings:[]};if(raw.package_kind!=='l2g_workbook_handoff_v1')return {status:'rejected',trusted:false,current:false,governedBuildAllowed:false,errors:[`package_kind must be l2g_workbook_handoff_v1; got ${JSON.stringify(raw.package_kind)}`],warnings:[]};if(raw.package_version!=='1.0')return {status:'rejected',trusted:false,current:false,governedBuildAllowed:false,errors:[raw.package_version==='1.7'?'top-level package_version 1.7 is an invalid wire encoding; Workbook Handoff contract release 1.7 uses wire package_version 1.0.':`unsupported wire package_version ${JSON.stringify(raw.package_version)}`],warnings:[]};if(raw.handoff_schema_enhancements_version==='1.7')return v3101ValidateCurrent(raw);if(raw.handoff_schema_enhancements_version==='1.6')return v3101ValidateLegacy16(raw);return {status:'rejected',trusted:false,current:false,governedBuildAllowed:false,errors:[raw.handoff_schema_enhancements_version==null?'missing handoff_schema_enhancements_version':`unsupported enhancement release ${JSON.stringify(raw.handoff_schema_enhancements_version)}; supported current release is 1.7 and explicit preview-only legacy release is 1.6.`],warnings:[]};}
-function v3101SourceIdentity(raw){return {package_kind:raw?.package_kind||'',package_version:raw?.package_version||'',contract_release:raw?.contract_manifest?.contract_release||raw?.package_integrity?.contract_release||'',enhancement_version:raw?.handoff_schema_enhancements_version||'',canonical_fingerprint:raw?.package_integrity?.canonical_fingerprint||'',generated_by:raw?.generated_by||'',generated_at:raw?.generated_at||'',workshop_version:raw?.gap_workshop_tool_version||raw?.contract_manifest?.producer?.version||'',session:raw?.session||{},source_lineage_contract:raw?.contract_manifest?.source_lineage_contract||{},source_documents:(raw?.source_documents_reviewed||[]).map(d=>({source_document_id:d.source_document_id||'',round_trip_source_record_id_v78:d.round_trip_source_record_id_v78||'',source_record_fingerprint_v78:d.source_record_fingerprint_v78||'',filename:d.filename||''}))};}
+function v3101SourceIdentity(raw){return {package_kind:raw?.package_kind||'',package_version:raw?.package_version||'',contract_release:raw?.contract_manifest?.contract_release||raw?.package_integrity?.contract_release||'',enhancement_version:raw?.handoff_schema_enhancements_version||'',canonical_fingerprint:raw?.package_integrity?.canonical_fingerprint||'',canonical_sha256_fingerprint:v3101CanonicalHandoffFingerprint(raw),generated_by:raw?.generated_by||'',generated_at:raw?.generated_at||'',workshop_version:raw?.gap_workshop_tool_version||raw?.contract_manifest?.producer?.version||'',session:raw?.session||{},source_lineage_contract:raw?.contract_manifest?.source_lineage_contract||{},source_documents:(raw?.source_documents_reviewed||[]).map(d=>({source_document_id:d.source_document_id||'',round_trip_source_record_id_v78:d.round_trip_source_record_id_v78||'',source_record_fingerprint_v78:d.source_record_fingerprint_v78||'',filename:d.filename||''}))};}
 function v3101CollectGovernance(raw){const actions=Array.isArray(raw?.workshop_action_register_v67?.actions)?raw.workshop_action_register_v67.actions.map(x=>JSON.parse(JSON.stringify(x))):[];const ownership=Array.isArray(raw?.optional_workshop_evidence_ownership_helper_v78?.records)?raw.optional_workshop_evidence_ownership_helper_v78.records.map(x=>JSON.parse(JSON.stringify(x))):[];const requests=[],providerFollowups=[];const walk=v=>{if(!v||typeof v!=='object')return;if(Array.isArray(v)){v.forEach(walk);return;}for(const [k,x] of Object.entries(v)){const nk=normalizeHeader(k);if(Array.isArray(x)&&['requests','evidence requests','ownership requests'].includes(nk))x.forEach(r=>requests.push(JSON.parse(JSON.stringify(r))));else if(Array.isArray(x)&&['provider followups','provider follow ups','followups','follow ups'].includes(nk))x.forEach(r=>providerFollowups.push(JSON.parse(JSON.stringify(r))));walk(x);}};walk(raw?.optional_workshop_evidence_ownership_helper_v78);const identity=v3101SourceIdentity(raw);return {actions,ownership_records:ownership,requests:requests,provider_followups:providerFollowups,source_identity:identity};}
 function v3101Join(v){return Array.isArray(v)?v.map(x=>typeof x==='string'?x:v3101Canonical(x)).join('; '):safeTrim(v);}
 function v3101ActionRow(r,id){return {'Action ID':r.action_id||r.id||'','Action Title':r.title||'','Description':r.description||'','Related Practice IDs':v3101Join(r.related_practices||r.practice_ids||r.practice_id),'Related Objective IDs':v3101Join(r.related_objectives||r.objective_ids||r.objective_id),'Action Owner':r.owner||r.action_owner||'','Supporting Owner':r.supporting_owner||'','Provider':r.provider||'','Service Reference':r.service_reference||r.service||'','Due Date':r.due_date||'','Priority':r.priority||'','Status':r.status||'','Blocker ID':r.blocker_id||'','Blocker Meaning':r.blocker_meaning||r.blocker||'','Dependencies':v3101Join(r.dependencies),'Related References':v3101Join(r.related_references||r.references),'Evidence Request ID':r.evidence_request_id||r.request_id||'','Submission Reference':r.submission_reference||'','Source Type':r.source_type||r.type||'','Source ID':r.source_id||'','Source Key':r.source_key||'','Source Label':r.source_label||r.source||'','Created At':r.created_at||'','Updated At':r.updated_at||'','Source Package Kind':id.package_kind,'Source Package Version':id.package_version,'Source Contract Release':id.contract_release,'Source Fingerprint':id.canonical_fingerprint,'Source Lineage JSON':v3101Canonical(id.source_lineage_contract||{}),'Source Record JSON':v3101Canonical(r)};}
@@ -6387,7 +6387,7 @@
  const workbookDoc=parseXml(await zip.file('xl/workbook.xml').async('text')),relDoc=parseXml(await zip.file('xl/_rels/workbook.xml.rels').async('text')),contentDoc=parseXml(await zip.file('[Content_Types].xml').async('text'));const sheets=Array.from(workbookDoc.getElementsByTagNameNS(MAIN_NS,'sheet'));const existing=sheets.find(s=>s.getAttribute('name')===name);if(existing){const rid=existing.getAttributeNS(REL_NS,'id')||existing.getAttribute('r:id');const rel=Array.from(relDoc.getElementsByTagNameNS(PKG_REL_NS,'Relationship')).find(r=>r.getAttribute('Id')===rid);if(!rel)throw new Error(`Governance helper sheet relationship missing for ${name}`);zip.file(sheetPathFromTarget(rel.getAttribute('Target')||''),buildWorksheetXml(rows,widths));return;}
  const ids=sheets.map(s=>Number(s.getAttribute('sheetId')||0)),nums=Object.keys(zip.files).map(p=>(p.match(/^xl\/worksheets\/sheet(\d+)\.xml$/)||[])[1]).filter(Boolean).map(Number),rids=Array.from(relDoc.getElementsByTagNameNS(PKG_REL_NS,'Relationship')).map(r=>(r.getAttribute('Id')||'').match(/^rId(\d+)$/)).filter(Boolean).map(m=>Number(m[1]));const sheetId=Math.max(0,...ids)+1,sheetNum=Math.max(0,...nums)+1,rid=`rId${Math.max(0,...rids)+1}`,path=`xl/worksheets/sheet${sheetNum}.xml`,target=`worksheets/sheet${sheetNum}.xml`;zip.file(path,buildWorksheetXml(rows,widths));const sh=workbookDoc.createElementNS(MAIN_NS,'sheet');sh.setAttribute('name',name);sh.setAttribute('sheetId',String(sheetId));sh.setAttributeNS(REL_NS,'r:id',rid);workbookDoc.getElementsByTagNameNS(MAIN_NS,'sheets')[0].appendChild(sh);const rel=relDoc.createElementNS(PKG_REL_NS,'Relationship');rel.setAttribute('Id',rid);rel.setAttribute('Type',WORKSHEET_REL_TYPE);rel.setAttribute('Target',target);relDoc.documentElement.appendChild(rel);const over=contentDoc.createElementNS(CONTENT_TYPES_NS,'Override');over.setAttribute('PartName',`/${path}`);over.setAttribute('ContentType','application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml');contentDoc.documentElement.appendChild(over);zip.file('xl/workbook.xml',serializeXml(workbookDoc));zip.file('xl/_rels/workbook.xml.rels',serializeXml(relDoc));zip.file('[Content_Types].xml',serializeXml(contentDoc));
 }
-async function v3101AddGovernanceSheets(zip,raw,validation){const g=v3101CollectGovernance(raw),id=g.source_identity;await v3101UpsertSheet(zip,'L2G Workshop Actions',v3101Rows(g.actions.map(r=>v3101ActionRow(r,id)),V3101_ACTION_HEADERS),[20,32,58,30,30,26,24,24,24,16,14,16,18,38,30,30,22,24,18,20,20,28,23,23,22,22,24,24,70,120]);await v3101UpsertSheet(zip,'L2G Evidence Ownership',v3101Rows(g.ownership_records.map(r=>v3101OwnershipRow(r,id)),V3101_OWNERSHIP_HEADERS),[24,28,24,24,24,24,26,18,50,24,45,40,24,24,22,24,20,22,20,18,16,28,23,23,22,24,70,120]);await v3101UpsertSheet(zip,'L2G Evidence Requests',v3101Rows(g.requests.map(r=>v3101RequestRow(r,id)),V3101_REQUEST_HEADERS),[22,24,20,22,18,24,24,24,16,18,60,22,22,22,24,70,120]);await v3101UpsertSheet(zip,'L2G Provider Follow-up',v3101Rows(g.provider_followups.map(r=>v3101FollowupRow(r,id)),V3101_FOLLOWUP_HEADERS),[24,22,24,20,22,24,24,24,16,18,60,22,22,22,24,70,120]);const identityRows=[['Field','Value'],['Validation status',validation.status],['Governed build allowed',validation.governedBuildAllowed?'Yes':'No'],['Package kind',id.package_kind],['Wire package version',id.package_version],['Contract release',id.contract_release],['Enhancement version',id.enhancement_version],['Canonical fingerprint',id.canonical_fingerprint],['Calculated fingerprint',validation.calculatedFingerprint||''],['Workshop version',id.workshop_version],['Actions preserved',String(g.actions.length)],['Ownership records preserved',String(g.ownership_records.length)],['Requests preserved',String(g.requests.length)],['Provider follow-ups preserved',String(g.provider_followups.length)],['Source lineage JSON',v3101Canonical(id)],['Guardrail','These sheets preserve Workshop-governed operational records and source identity. They do not establish implementation status, evidence sufficiency, readiness, assessment, compliance, certification, scoring, technical accuracy, approval, or client release.']];await v3101UpsertSheet(zip,'L2G Handoff Identity',identityRows,[34,120]);return g;}
+async function v3101AddGovernanceSheets(zip,raw,validation){const g=v3101CollectGovernance(raw),id=g.source_identity;await v3101UpsertSheet(zip,'L2G Workshop Actions',v3101Rows(g.actions.map(r=>v3101ActionRow(r,id)),V3101_ACTION_HEADERS),[20,32,58,30,30,26,24,24,24,16,14,16,18,38,30,30,22,24,18,20,20,28,23,23,22,22,24,24,70,120]);await v3101UpsertSheet(zip,'L2G Evidence Ownership',v3101Rows(g.ownership_records.map(r=>v3101OwnershipRow(r,id)),V3101_OWNERSHIP_HEADERS),[24,28,24,24,24,24,26,18,50,24,45,40,24,24,22,24,20,22,20,18,16,28,23,23,22,24,70,120]);await v3101UpsertSheet(zip,'L2G Evidence Requests',v3101Rows(g.requests.map(r=>v3101RequestRow(r,id)),V3101_REQUEST_HEADERS),[22,24,20,22,18,24,24,24,16,18,60,22,22,22,24,70,120]);await v3101UpsertSheet(zip,'L2G Provider Follow-up',v3101Rows(g.provider_followups.map(r=>v3101FollowupRow(r,id)),V3101_FOLLOWUP_HEADERS),[24,22,24,20,22,24,24,24,16,18,60,22,22,22,24,70,120]);const identityRows=[['Field','Value'],['Validation status',validation.status],['Governed build allowed',validation.governedBuildAllowed?'Yes':'No'],['Package kind',id.package_kind],['Wire package version',id.package_version],['Contract release',id.contract_release],['Enhancement version',id.enhancement_version],['Canonical fingerprint',id.canonical_fingerprint],['Canonical SHA-256 fingerprint',id.canonical_sha256_fingerprint],['Calculated fingerprint',validation.calculatedFingerprint||''],['Workshop version',id.workshop_version],['Actions preserved',String(g.actions.length)],['Ownership records preserved',String(g.ownership_records.length)],['Requests preserved',String(g.requests.length)],['Provider follow-ups preserved',String(g.provider_followups.length)],['Source lineage JSON',v3101Canonical(id)],['Guardrail','These sheets preserve Workshop-governed operational records and source identity. They do not establish implementation status, evidence sufficiency, readiness, assessment, compliance, certification, scoring, technical accuracy, approval, or client release.']];await v3101UpsertSheet(zip,'L2G Handoff Identity',identityRows,[34,120]);return g;}
 function v3101SheetPath(workbookDoc,relDoc,name){const sh=Array.from(workbookDoc.getElementsByTagNameNS(MAIN_NS,'sheet')).find(s=>s.getAttribute('name')===name);if(!sh)return'';const rid=sh.getAttributeNS(REL_NS,'id')||sh.getAttribute('r:id');const rel=Array.from(relDoc.getElementsByTagNameNS(PKG_REL_NS,'Relationship')).find(r=>r.getAttribute('Id')===rid);return rel?sheetPathFromTarget(rel.getAttribute('Target')||''):'';}
 async function v3101ReadRows(zip,name){const wb=parseXml(await zip.file('xl/workbook.xml').async('text')),rels=parseXml(await zip.file('xl/_rels/workbook.xml.rels').async('text')),path=v3101SheetPath(wb,rels,name);if(!path||!zip.file(path))return[];const shared=await loadSharedStrings(zip),doc=parseXml(await zip.file(path).async('text')),out=[];for(const row of Array.from(doc.getElementsByTagNameNS(MAIN_NS,'row'))){const values=[];for(const c of Array.from(row.getElementsByTagNameNS(MAIN_NS,'c'))){const ref=splitCellRef(c.getAttribute('r')||'');if(ref)values[ref.colNum-1]=cellValue(c,shared);}out.push(values.map(v=>v??''));}if(out.length<2)return[];const h=out[0].map(safeTrim);return out.slice(1).filter(r=>r.some(v=>safeTrim(v))).map(r=>Object.fromEntries(h.map((x,i)=>[x,r[i]??''])));}
 function v3101ParseRawCell(row){const text=row['Source Record JSON'];if(!safeTrim(text))return{};return v3101JsonNoDuplicateKeys(text);}
@@ -6417,7 +6417,7 @@
 const v3101BaseAddL2GSheets=addL2GSheets;addL2GSheets=async function(zip,plan){await v3101BaseAddL2GSheets(zip,plan);if(!state.handoffRaw||!state.v3101HandoffValidation?.governedBuildAllowed)throw new Error('Governed workbook generation requires a fully trusted current Workbook Handoff 1.0/1.7 package.');const g=await v3101AddGovernanceSheets(zip,state.handoffRaw,state.v3101HandoffValidation);plan.workshopActionsPreserved=g.actions.length;plan.evidenceOwnershipPreserved=g.ownership_records.length;plan.governanceSourceFingerprint=g.source_identity.canonical_fingerprint;};
 const v3101BaseDownloadWorkbook=downloadPopulatedWorkbook;downloadPopulatedWorkbook=async function(){if(!state.v3101HandoffValidation?.governedBuildAllowed)throw new Error('Workbook download is blocked because the loaded Handoff is not a fully trusted current 1.0/1.7 package.');return v3101BaseDownloadWorkbook();};
 const v3101BaseReviewHandler=handleReviewWorkbookFile;handleReviewWorkbookFile=async function(file){await v3101BaseReviewHandler(file);state.v3101GovernanceExtract=await v3101ReadGovernance(state.reviewWorkbookZip);await buildExtractPreviewIfReady();};
-const v3101BaseExtractPlan=buildExtractPlan;buildExtractPlan=function(workbookInfo,reviewRows,compareHandoff,templateRows){const plan=v3101BaseExtractPlan(workbookInfo,reviewRows,compareHandoff,templateRows),ext=v3101PreservationExtension(state.v3101GovernanceExtract,state.compareHandoffRaw);delete plan.packageObj.workshop_governance_preservation_v1;if(ext){plan.packageObj.workbook_source=plan.packageObj.workbook_source||{};plan.packageObj.workbook_source.workshop_governance_preservation_v1=ext;const check=v3101ValidatePreservationPackage(plan.packageObj);if(check.status==='rejected')throw new Error(`Workbook Merge preservation extension validation failed: ${check.errors.join(' | ')}`);if(check.status==='blocked')plan.warnings=[...(plan.warnings||[]),...check.blockers.map(x=>`Workshop governance reconciliation blocked: ${x}`)];plan.report+=`\n## v3.10.1 Workshop Action and Ownership Preservation\n\nActions preserved: ${ext.record_counts.actions}\nEvidence ownership records preserved: ${ext.record_counts.evidence_ownership_records}\nSource Handoff canonical fingerprint: ${ext.source_package_identity.canonical_fingerprint}\nPreservation fingerprint: ${ext.preservation_fingerprint}\nExtension path: workbook_source.workshop_governance_preservation_v1\nHelper sheets: L2G Workshop Actions; L2G Evidence Ownership; L2G Evidence Requests; L2G Provider Follow-up; L2G Handoff Identity\n\nThe canonical schema 1.0 preservation extension carries governed source/workbook projections, stable record IDs, SHA-256 record fingerprints, exact counts, source-Handoff identity linkage, and the required non-mutation guardrails. It is reconciliation evidence only and does not automatically create, overwrite, restore, or delete Workshop operational records.\n`;}return plan;};
+const v3101BaseExtractPlan=buildExtractPlan;buildExtractPlan=function(workbookInfo,reviewRows,compareHandoff,templateRows){const plan=v3101BaseExtractPlan(workbookInfo,reviewRows,compareHandoff,templateRows),ext=v3101PreservationExtension(state.v3101GovernanceExtract,state.compareHandoffRaw);delete plan.packageObj.workshop_governance_preservation_v1;if(ext){const storedSourceFingerprint=safeTrim(state.v3101GovernanceExtract?.identity?.['Canonical SHA-256 fingerprint']);if(state.compareHandoffRaw&&storedSourceFingerprint&&storedSourceFingerprint!==ext.source_package_identity.canonical_fingerprint)throw new Error('Workbook Merge preservation extension validation failed: source Handoff fingerprint mismatch');plan.packageObj.workbook_source=plan.packageObj.workbook_source||{};plan.packageObj.workbook_source.workshop_governance_preservation_v1=ext;const check=v3101ValidatePreservationPackage(plan.packageObj);if(check.status==='rejected')throw new Error(`Workbook Merge preservation extension validation failed: ${check.errors.join(' | ')}`);if(check.status==='blocked')plan.warnings=[...(plan.warnings||[]),...check.blockers.map(x=>`Workshop governance reconciliation blocked: ${x}`)];plan.report+=`\n## v3.10.1 Workshop Action and Ownership Preservation\n\nActions preserved: ${ext.record_counts.actions}\nEvidence ownership records preserved: ${ext.record_counts.evidence_ownership_records}\nSource Handoff canonical fingerprint: ${ext.source_package_identity.canonical_fingerprint}\nPreservation fingerprint: ${ext.preservation_fingerprint}\nExtension path: workbook_source.workshop_governance_preservation_v1\nHelper sheets: L2G Workshop Actions; L2G Evidence Ownership; L2G Evidence Requests; L2G Provider Follow-up; L2G Handoff Identity\n\nThe canonical schema 1.0 preservation extension carries governed source/workbook projections, stable record IDs, SHA-256 record fingerprints, exact counts, source-Handoff identity linkage, and the required non-mutation guardrails. It is reconciliation evidence only and does not automatically create, overwrite, restore, or delete Workshop operational records.\n`;}return plan;};
 const v3101BaseBuildReport=buildReport;buildReport=function(workbookInfo,handoff,importRows,skippedFields,warnings,unmatched,visibleContextRows,plan={}){return v3101BaseBuildReport(workbookInfo,handoff,importRows,skippedFields,warnings,unmatched,visibleContextRows,plan)+`\n## v3.10.1 Strict Handoff and Governance Preservation\n\nStrict Handoff status: ${state.v3101HandoffValidation?.status||'Not evaluated'}\nWorkbook Handoff identity: contract release 1.7, wire package version 1.0\nWorkshop actions preserved: ${plan.workshopActionsPreserved||0}\nEvidence ownership records preserved: ${plan.evidenceOwnershipPreserved||0}\nSource fingerprint: ${plan.governanceSourceFingerprint||''}\nGovernance helper sheets are visible and carry exact source-record JSON plus deterministic record identity.\n`;};
 const v3101BaseClear=clearAll;clearAll=function(){v3101BaseClear();Object.assign(state,{v3101HandoffValidation:null,v3101CompareValidation:null,v3101GovernanceExtract:null,v3101LastGeneratedWorkbook:null});$('v3101ValidationPanel')?.remove();$('v3101CompareValidationPanel')?.remove();};
 if(typeof v38RoadmapMarkdown==='function'){const v3101BaseRoadmap=v38RoadmapMarkdown;v38RoadmapMarkdown=function(){return v3101BaseRoadmap().replace(/Current release: L2G-BM v3\.10(?!\.1)/g,'Current release: L2G-BM v3.10.1')+`\n\n## Corrective release v3.10.1\n\nStrict Workbook Handoff contract-release 1.7 / wire-package 1.0 validation; duplicate-key-safe parsing; exact identity, shape, fingerprint, practice/objective, and source-lineage checks; and lossless Workshop action/evidence-ownership preservation through visible workbook helper sheets and Workbook Merge 1.1 at the canonical nested preservation-extension path. Builder/Merger v3.9 remains reserved for Advisor and Client Delivery Profiles.\n`;};}
