# Security and Privacy Design — v1.8.6

All review processing remains offline. Segregation-of-duties rules are enforced against locally asserted role assignments. Fingerprints detect content drift after submission or approval. Revocation is append-oriented and requires a reason. Existing import, DOCX, identity, path, macro, and overwrite protections remain unchanged. No telemetry or external identity lookup is performed.
