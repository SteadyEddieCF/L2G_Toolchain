# Import and Export Specification — v1.8.2

Portfolio and module packages advance to package version 1.2 while retaining `cmmc_l2_ssp_portfolio_foundation_v1` and `cmmc_l2_ssp_module_foundation_v1`. Exports include source record IDs and versions, fingerprint algorithm/value/time, inheritance state, supplements, overrides, stale counts, and override counts. Existing backup, CRM, Word Review, Workshop handoff, and SSP return identities remain unchanged.
