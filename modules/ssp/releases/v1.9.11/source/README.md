# v1.9.11 materialization source

The text segments in this directory reconstruct the exact v1.9.11 standalone runtime and additive working-data schema. `materialize.py` verifies encoded, compressed, payload, baseline, output, registry, and registry-schema SHA-256 identities before writing generated artifacts. No Builder/Merger sidecar or cross-tool package is stored here.
