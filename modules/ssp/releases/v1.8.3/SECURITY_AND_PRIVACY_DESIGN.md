# Security and Privacy Design — v1.8.3

Processing remains offline. Imported values are normalized, length-limited, enumerated, and source-graph validated. Impact field names are allowlisted. Rationale is mandatory for preserve, defer, and escalate. UI rendering uses text nodes rather than imported HTML. Fingerprints remain deterministic change detectors rather than cryptographic signatures or approvals.
