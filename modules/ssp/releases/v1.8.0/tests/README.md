# v1.8.0 Tests

Run `python3 tests/test_ssp_v180_static.py` from the development workspace. The DOM runtime suite uses Node 24 and `happy-dom` 18.0.1. Repository CI supplies the authoritative Playwright/axe, visual, and Windows file gates.
