# Import and Export Specification — v1.8.8

Full portfolio exchange preserves `changeDecisionRegister`. Module exchange includes explicit register entries scoped to the exported module. Imports fail closed on duplicate stable IDs with different content. The consolidated register exports JSON package kind `cmmc_l2_ssp_change_decision_register_v1` version `1.8` and an equivalent UTF-8 CSV.
