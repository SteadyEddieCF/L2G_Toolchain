# Portfolio Exchange Specification — v1.8.7

Portfolio/module exchange remains backward-compatible with version 1.5 safe-only reconciliation. Complete foundation export advances to version 1.7 and preserves `approvedBaselines` and `baselineEvents`. Baseline packages are separate immutable authoring artifacts and are not accepted as automatic portfolio replacements.
