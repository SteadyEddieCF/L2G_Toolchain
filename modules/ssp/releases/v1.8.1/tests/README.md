# v1.8.1 Tests

Run `python3 tests/test_ssp_v181_static.py` and `node tests/test_ssp_v181_runtime.mjs` from the development workspace. The DOM suite uses Node and Happy DOM. Repository CI supplies Playwright/axe, visual, and Windows `file://` gates.
