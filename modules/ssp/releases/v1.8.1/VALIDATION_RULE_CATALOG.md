# Validation Rule Catalog — v1.8.1

Active rules: supported schema and mode; stable IDs; one root anchor; unique module IDs; valid parent graph; no cycles; known requirement IDs; unique requirement record IDs; unique module/requirement pairs; exactly 110 completed records per module; allowed enums; required Not-applicable rationale; required inherited source; valid source module; text/list limits; and maximum 50 modules / 5,500 requirement records.

Bulk actions exclude Not applicable and inherited applicability because each requires record-specific rationale or source metadata. Pending decisions remain visible readiness gaps. No rule makes an assessment, certification, contract, or legal determination.
