# Security and Privacy Design — v1.8.4

The runtime remains local/offline with no external scripts, telemetry, or remote persistence. CRM CSV parsing occurs in the browser. Imported HTML is not rendered. Stable identifiers and fingerprints support traceability without sending content externally. Conflicting rows are never silently applied, and the UI downloads a JSON backup before applying safe rows.
