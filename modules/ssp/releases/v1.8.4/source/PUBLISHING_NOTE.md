# Publishing Note

The repository branch contains deterministic governed source and metadata. The draft pull-request materialization workflow reconstructed and committed the generated v1.8.4 release files for independent review and validation. The complete deliverables ZIP is provided separately to the release owner.

The post-materialization validation trigger is intentionally authored by the repository owner so the complete Playwright and toolchain workflows run against the generated runtime rather than the pre-materialization branch state.
