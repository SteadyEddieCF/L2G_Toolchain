# Workshop Tests

Store reusable Workshop static/runtime harnesses here. Release-specific regression results belong under `modules/workshop/releases/<version>/`.

Native Windows Chrome/Edge file-origin persistence remains a required manual acceptance check when the managed test environment cannot exercise `file://` localStorage.
