# SSP v1.9.17 RG-4 Evidence-History Harness Reconciliation

This tests-and-fixtures-only package implements issue #107 from exact protected-main baseline `3be4153c45cb6534954592a6e4ff1cfda87c8bb4`.

## What changed

- Added an exact Workshop Handoff 1.0 fixture with 110 controls.
- Added the unsupported legacy synthetic history seed as a negative fixture.
- Added a shared Playwright helper that creates a legitimate RG-4 record through exact sidecar/DOCX validation and explicit local acceptance.
- Added populated-history, empty-history, SSP Return no-leakage, unsupported-seed normalization, and stale-acknowledgement tests.
- Added native Windows Chromium `file://` coverage.
- Preserved and re-runs the existing SSP v1.9.17 current, blocked, incomplete, duplicate, retry, supersession, backup/restore, security, accessibility, and UI tests.
- Added a focused CI workflow and shared Playwright registration.

## Root cause

The old seed was not a canonical SSP RG-4 record. It omitted both supported package-fingerprint locations. SSP's normalizer therefore removed it. Direct state and local-storage injection are not supported record-creation workflows.

## Evidence generated by CI

The focused and shared Playwright runs attach:

- unsupported synthetic-history normalization evidence;
- populated-history Workshop isolation evidence;
- empty-history Workshop isolation evidence;
- SSP Return no-history-leakage evidence;
- supported stale-acknowledgement evidence; and
- Windows file-origin isolation evidence.

Exact generated sidecar, DOCX, return-package, record, and fixture identities are included in those JSON attachments.

## Authority and boundaries

No SSP or adjacent-tool runtime is modified. No working-data schema, current pointer, contract registry entry, or historical suite snapshot is changed. The RG-4 sidecar route remains `proposal`; this package does not promote it and makes no CMMC readiness, compliance, assessment, certification, scoring, evidence-sufficiency, technical-accuracy, authenticated-identity, signature, approval, or client-release claim.
